// TODO: Create an interface for Issue related to the Issue API
export interface Issue {
    issueId: number
    issueTopic: string
    issueType: string
    issueStatus: string
    issueBy: string
    issueDate: string
}